"use client";
import { useState } from "react"; import { useTranslations } from "next-intl"; import { promoteAdminToSuperAdminAction } from "@/lib/actions/adminManagement";
export function PromoteAdminDialog({adminId}:{adminId:string}) { const t=useTranslations("adminManagement.detail"); const [confirmed,setConfirmed]=useState(false); return <section className="mt-5 rounded-xl border border-warning p-4"><h2 className="font-extrabold">{t("promoteTitle")}</h2><p className="mt-1 text-sm text-text-secondary">{t("promoteDescription")}</p><label className="mt-3 flex min-h-11 items-center gap-3"><input type="checkbox" checked={confirmed} onChange={(event)=>setConfirmed(event.target.checked)}/><span>{t("promoteConfirm")}</span></label><form action={promoteAdminToSuperAdminAction.bind(null,adminId)}><button disabled={!confirmed} className="min-h-11 rounded-md border border-warning px-5 font-bold disabled:opacity-50">{t("promote")}</button></form></section>; }
