/** Tagline/description live in messages/*.json under `site.*` — these are locale-independent identifiers only. */
export const site = {
  name: "FutureTutor",
  url: "https://www.futuretutor.ca",
  country: "Canada",
  twitterHandle: "@futuretutor",
} as const;
